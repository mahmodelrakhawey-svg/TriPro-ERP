# 🛠️ دليل المطور (Developer Guide) - TriPro ERP

## 📋 نظرة عامة
TriPro ERP هو نظام تخطيط موارد المؤسسات مبني باستخدام تقنيات الويب الحديثة. يهدف هذا الدليل لمساعدة المطورين على فهم البنية التحتية والمساهمة في المشروع.

## 🛠️ التقنيات المستخدمة (Tech Stack)
- **Frontend:** React 18, TypeScript, Vite
- **Styling:** Tailwind CSS
- **State Management:** React Context API, TanStack Query
- **Backend/Database:** Supabase (PostgreSQL)
- **Icons:** Lucide React
- **Charts:** Recharts
- **Validation:** Zod
- **Testing:** Vitest

## 📂 هيكل المشروع (Project Structure)
```text
/src
  /components    # مكونات واجهة المستخدم القابلة لإعادة الاستخدام
  /context       # إدارة الحالة العامة (Auth, Accounting, Toast)
  /hooks         # Custom React Hooks
  /modules       # وحدات النظام الرئيسية (المحاسبة، المبيعات، إلخ)
  /services      # الاتصال بالخدمات الخارجية وقاعدة البيانات
  /types         # تعريفات TypeScript
  /utils         # دوال مساعدة (Helpers, Formatters, Validators)
```

## 🚀 إعداد بيئة التطوير
1. **المتطلبات:** Node.js v16+, npm/yarn.
2. **التثبيت:** `npm install`
3. **متغيرات البيئة:** انسخ `.env.example` إلى `.env` وأضف مفاتيح Supabase.
4. **التشغيل:** `npm run dev`

## 🧪 الاختبارات
نستخدم **Vitest** لاختبارات الوحدة.
- تشغيل الاختبارات: `npm test`
- تغطية الكود: `npm run coverage`

## 📝 معايير الكود
- استخدم TypeScript دائماً.
- تجنب `any` قدر الإمكان.
- استخدم `showToast` بدلاً من `alert`.
- وثق الدوال المعقدة باستخدام JSDoc.