-- =================================================================
-- إضافة عمود الملاحظات لتفاصيل الجرد (للمطبخ وغيره)
-- =================================================================

ALTER TABLE public.inventory_count_items 
ADD COLUMN IF NOT EXISTS notes TEXT;

SELECT '✅ تم إضافة عمود الملاحظات لجدول تفاصيل الجرد.' as status;