# 🚀 استراتيجية نشر وتحديث النظام (Deployment Strategy)

## الهيكلية (Architecture)
نستخدم نظام **Single-tenant**، مما يعني أن لكل عميل قاعدة بيانات ومشروع Supabase منفصل خاص به.

### لماذا؟
1. **أمان تام:** بيانات العميل "أ" معزولة فيزيائياً عن العميل "ب".
2. **استقرار:** خطأ في بيانات عميل لا يؤثر على الآخرين.
3. **مرونة:** يمكن تخصيص قاعدة بيانات لعميل معين دون التأثير على الباقين.

---

## كيف تدير 5 عملاء بكود واحد؟

### 1. الكود (Frontend)
*   لديك **مستودع Git واحد** (Repository) يحتوي على كود React.
*   عندما تريد تحديث البرنامج لجميع العملاء:
    1.  قم بتعديل الكود محلياً.
    2.  ارفع التعديلات (`git push`).
    3.  منصة الاستضافة (مثل Vercel) ستقوم تلقائياً بسحب الكود الجديد وبناء 5 نسخ مختلفة، واحدة لكل عميل، بناءً على إعدادات الربط.

### 2. قاعدة البيانات (Backend)
*   لكل عميل مشروع Supabase خاص (URL & Key مختلف).
*   **عند انضمام عميل جديد:**
    1.  أنشئ مشروع Supabase جديد.
    2.  نفذ ملف `database/full_schema.sql` في الـ SQL Editor.
    3.  (اختياري) نفذ ملف `database/seed_dummy_data.sql` إذا أراد بيانات تجريبية.

*   **عند تحديث قاعدة البيانات (مثلاً إضافة جدول جديد):**
    *   يجب عليك تنفيذ كود SQL التحديثي يدوياً في مشاريع Supabase الـ 5 الخاصة بالعملاء.
    *   *نصيحة:* احتفظ بملفات التحديث في مجلد `database/migrations` (مثلاً `01_add_credit_limit.sql`) لتعرف ما تم تنفيذه.

---

## ملفات هامة
*   `database/full_schema.sql`: المخطط الكامل لإنشاء قاعدة بيانات لعميل جديد.
*   `database/seed_dummy_data.sql`: بيانات وهمية للعرض (Demo).