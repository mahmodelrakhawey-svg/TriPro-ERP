# 📖 التوثيق الشامل والمرجع المعماري لمشروع TriPro-ERP
> **تاريخ آخر تحديث:** سبتمبر 2026  
> **الإصدار الحالي:** V52.0 Enterprise Production Ready  
> **حالة النظام:** مجرب ومثبت تشغيلياً في بيئة الإنتاج المباشرة (شركة حلواني لينزا + بيئة الإنتاج العامة).

---

## 1. نظرة عامة على المشروع (Project Overview)
**TriPro ERP** هو نظام مؤسسي متكامل لإدارة الموارد السحابية (Enterprise Multi-Tenant SaaS ERP) مبني باستخدام:
- **الواجهة الأمامية (Frontend):** React 18, TypeScript, Vite, TailwindCSS, Ant Design, Lucide Icons.
- **قاعدة البيانات والواجهة الخلفية (Backend & DB):** PostgreSQL / Supabase, مع دوال PL/pgSQL مخزنة (RPCs) لضمان المعاملات المالية الذرية (ACID Transactions)، وسياسات أمان صارمة على مستوى السجلات (RLS).
- **المعمارية المالية:** محرك قيود مزدوج (Double-Entry General Ledger) متوافق مع معايير المحاسبة والضرائب المصرية والفاتورة الإلكترونية (ETA).

---

## 2. المعمارية الموزعة للسياق المركزي (Domain Facade Architecture)
تم بنجاح تفكيك وإعادة هيكلة الملف المركزي الضخم `context/AccountingContext.tsx` باستخدام **نمط الواجهة الموحدة (Facade Pattern)** لحل مشكلة إعادة التصيير غير الضروري (Re-renders) مع الحفاظ التام على التوافق الرجعي 100%:

### أ. مجلد واجهات النطاقات (`context/domains/`):
تم عزل الـ State والدوال التشغيلية في سياقات وهوكات متخصصة:
1. **`CustomerContext.tsx` (`useCustomerDomain`):** إدارة العملاء، الأرصدة، حدود الائتمان، وفئات التسعير.
2. **`SupplierContext.tsx` (`useSupplierDomain`):** إدارة الموردين، تقييماتهم، وأرصدة حساباتهم.
3. **`ProductContext.tsx` (`useProductDomain`):** الأصناف، الباركود، وحدات القياس (UOM)، وتكاليف الـ WAC.
4. **`AccountingSettingsContext.tsx` (`useAccountingSettingsDomain`):** إعدادات الضرائب، العملات، الفروع، وتخصيصات المنشأة.
5. **`SalesContext.tsx` (`useSalesDomain`):** الفواتير، عروض الأسعار، وأوامر البيع.
6. **`BankingContext.tsx` (`useBankingDomain`):** الخزائن، البنوك، أوراق القبض والدفع (الشيكات).
7. **`GeneralLedgerContext.tsx` (`useGeneralLedgerDomain`):** دليل الحسابات، قيود اليومية، مراكز التكلفة، وميزان المراجعة.

### ب. بقاء `AccountingContext.tsx` كواجهة موحدة (Facade):
- يظل ملف `context/AccountingContext.tsx` كما هو، ويعيد تجميع الهوكات السبعة وتصديرها.
- **النتيجة الحتمية:** كافة الشاشات القديمة (أكثر من 100 شاشة) تستدعي `useAccounting()` بنفس المسار والأسماء القديمة بالملي دون أي كسر.
- الشاشات الجديدة أو المحسنة يمكنها استدعاء الهوك التخصصي مباشرة (مثل `useProductDomain()`) للحصول على أداء فائق وسرعة رندر لحظية.
- تمت تغطية هذا النمط باختبار تكاملي دائم في `tests/domain_facade_hooks.test.ts` (ناجح 100%).

---

## 3. القطاعات التخصصية وتحديثات الأمان والتشغيل
يغطي النظام 7 قطاعات تجارية وصناعية كبرى، مع أحدث التحديثات المنفذة:

### 1. المطاعم والكافيهات (`modules/restaurant/`):
- **نظام كارت المشرف والباركود:** تم تزويد شاشة كاشير المطعم (`PosScreen.tsx` و `OrderSummary.tsx`) بنظام مصادقة المشرف المعتمد بالباركود ورمز الـ PIN السري (`SupervisorPinModal` و `SupervisorBadgePrintModal`):
  - حظر إلغاء أي صنف تم حفظه أو إرساله للمطبخ (`savedQuantity > 0`) إلا بإذن المشرف.
  - حظر إلغاء الطاولات المشغولة إلا بإذن المشرف.
  - حظر تطبيق الخصومات اليدوية إلا بعد مسح كارت المشرف.
  - إضافة زر طباعة كروت المشرفين من شريط الأدوات العلوي للكاشير.
- **محرك الطلبات ورسوم الخدمة:** حساب دقيق لرسوم الصالة (12%)، وضريبة القيمة المضافة (14%) التي تطبق قانوناً على (الصافي + الخدمة)، وتتبع الطلبات لشاشات المطبخ (KDS).

### 2. الملاعب والأندية الرياضية (`modules/stadium/`):
- **محرك منع تداخل الحجوزات (Double Booking Prevention):** خوارزمية `checkBookingConflict` لمنع حجز نفس المرفق أو الملعب في أوقات متقاطعة، مع السماح بالحجوزات المتتالية (Back-to-Back).
- **إصلاح انحراف التوقيت الصيفي (DST Bug Fix):** تم تصحيح دالة `calcSubscriptionEndDate` في `stadiumHelpers.ts` بالاعتماد على التقويم المحلي لمنع انحراف التواريخ بمقدار يوم واحد عند التحول للتوقيت الصيفي.
- **القيود المحاسبية التلقائية:** توليد قيود الاشتراكات، الحجوزات، وإنشاء شيكات القبض في جدول `cheques`.

### 3. المقاولات والمشروعات (`modules/construction/`):
- **المستخلصات التراكمية (Progress Billings):** احتساب نسب الإنجاز التراكمية من جدول المقايسة (BOQ).
- **الاستقطاعات القانونية:** خصم ضمان الأعمال (Retention 10%)، استقطاع نسبة الدفعة المقدمة، ضريبة القيمة المضافة (14%) على الصافي، وضريبة الخصم والأرباح التجارية (WHT 1%).

### 4. الموارد البشرية والرواتب (`modules/hr/`):
- **مكافأة نهاية الخدمة طبقاً لقانون العمل:** احتساب مدد الخدمة الدقيقة بالسنة والشهر واليوم.
- **تدرج نسب الاستقالة:** (< سنتين: 0%، 2-5 سنوات: 33.3%، 5-10 سنوات: 66.7%، 10+ سنوات: 100%).
- **إنهاء الخدمة من الشركة:** 100% (نصف شهر عن أول 5 سنوات وشهر كامل لما بعد ذلك)، مع تسوية رصيد الإجازات والسلف.

### 5. كاشير الهايبر ماركت والتجزئة (`modules/retail/`):
- دعم الموازين الإلكترونية وقراءة الباركود المتغير التلقائي.
- محرك العروض والخصومات الترويجية (Promotion Engine).
- جرد الوردية الأعمى (Blind Shift Close) ومنع التلاعب النقدي.

### 6. المستشفيات والقطاع الطبي (`modules/hims/`):
- محرك فواتير الزيارات والإقامة الطبية (`HospitalBillingEngine`).
- إدارة خروج المرضى والتأكد من تصفية المديونية (`DischargeManager`).

### 7. التصنيع وإدارة التكاليف (`modules/manufacturing/`):
- شجرة المنتج (BOM) وأوامر التشغيل.
- حسابات نسب الهالك والمردود في المجازر والتصنيع الغذائي (`butcheringYield.test.ts`).

---

## 4. منظومة الاختبارات المؤتمتة (Automated Test Suites)
يمتلك النظام **22 ملف اختبار مؤتمت** تحتوي على **110+ اختبارات فحص شاملة وناجحة بنسبة 100%**:

| المجال | ملف الاختبار | عدد الحالات | التغطية المحاسبية والتقنية |
| :--- | :--- | :---: | :--- |
| **دفتر اليومية المالي** | `journalEntryBalance.test.ts` | 4 | توازن المدين والدائن بالملي |
| **صمام الأمان المالي** | `financialSafetyNet.test.ts` | 6 | منع إدخال أي قيد غير متوازن لقاعدة البيانات |
| **الفاتورة والضرائب** | `salesInvoiceCalculation.test.ts` | 5 | احتساب الـ VAT والخصومات |
| **عزل الشركات والأمان** | `organization_isolation_rls.test.ts` | 3 | سياسات RLS لمنع تسرب البيانات بين الشركات |
| **الفاتورة الإلكترونية** | `etaCanonicalization.test.ts` | 3 | مطابقة معايير التجزئة والتشفير لمنظومة الضرائب المصرية |
| **وحدات القياس** | `uomConversion.test.ts` | 5 | معاملات التحويل وتكاليف الوحدات الصغرى والكبرى |
| **هالك المجازر والتصنيع**| `butcheringYield.test.ts` | 3 | توزيع تكلفة الذبيحة على القطوعات ونسب الهالك |
| **محرك الحسابات الموحد** | `accounting_engine.test.ts` | 12 | دوال الحسابات الرياضية والمطابقة |
| **واجهات النطاقات Facade**| `domain_facade_hooks.test.ts` | 4 | استقرار تقسيم سياقات الـ AccountingContext |
| **مطابقة الشاشات** | `cross_screen_reconciliation.test.ts` | 5 | تطابق كشوف الحسابات وموازين المراجعة |
| **التسلسل والترقيم** | `sequence_and_optimizations.test.ts` | 5 | أرقام الفواتير المتسلسلة بدون فجوات |
| **القيود العكسية والرقابة**| `reversal_and_security.test.ts` | 3 | منع التعديل اليدوي وتوليد قيود تسوية عكسية |
| **التحقق من المدخلات** | `journal_validation.test.ts` | 5 | فحص الحسابات المدينة والدائنة ومراكز التكلفة |
| **المطاعم والورديات** | `tests/restaurant/restaurant_pos_flow.test.ts` | 8 | الصالة والتيك أواي ودوال الـ RPC للورديات والطلبات |
| **الملاعب وحجوزات النوادي**| `tests/stadium/stadium_engine.test.ts` | 9 | خوارزمية منع التداخل، الإشغال، والقيود المحاسبية |
| **المقاولات والمستخلصات** | `tests/construction/progress_billing.test.ts` | 4 | مستخلصات BOQ، ضمان الأعمال 10%، وضرائب المقاولات |
| **الموارد البشرية ونهاية الخدمة**| `tests/hr/end_of_service.test.ts` | 8 | معادلات قانون العمل، تدرج الاستقالة، والإجازات والسلف |
| **كاشير الهايبر ماركت** | `modules/retail/` tests | 14 | محرك العروض، الباركود، ومنع الدفع الناقص |
| **إدارة الصلاحيات (RBAC)**| `modules/admin/PermissionsManager.test.tsx` | 3 | فحص تكامل استدعاء دوال الصلاحيات بقاعدة البيانات |
| **القطاع الطبي** | `modules/hims/` tests | 2 | خروج المرضى ومحرك فواتير المستشفيات |

---

## 5. بروتوكول العمل الآمن وتوزيع البيئات (Multi-Environment Protocol)
طبقا لما تم تثبيته في `SAFE_WORKFLOW_GUIDE.md`:

### خريطة البيئات:
1. **بيئة التطوير النشطة (شركة حلواني لينزا):**
   - **المجلد المحلي:** `C:\Users\pc\Desktop\TriPro-ERP`
   - **قاعدة البيانات:** `jsgmrspnthtlsracbmcq.supabase.co`
   - **نطاق Vercel المباشر:** [tri-pro-erp-lenza.vercel.app](https://tri-pro-erp-lenza.vercel.app)
2. **بيئة الإنتاج العامة (Master Production):**
   - **المجلد المحلي:** `F:\نسخه منضبطه من البرنامج\نسخه 7 سبتمبر 2026\TriPro-Production`
   - **قاعدة البيانات:** `pjvphxfschfllpawfewn.supabase.co`
   - **نطاق Vercel المباشر:** [tri-pro-erp.vercel.app](https://tri-pro-erp.vercel.app)

### سكريبت النقل الآمن (Robocopy Safe Sync):
يتم نقل التعديلات من التطوير إلى الإنتاج مع استبعاد ملفات البيئة والمستودعات نهائياً:
```powershell
$SourceDir = "C:\Users\pc\Desktop\TriPro-ERP"
$TargetDir = "F:\نسخه منضبطه من البرنامج\نسخه 7 سبتمبر 2026\TriPro-Production"
$ExcludeFiles = @(".env", ".env.*", "npm-debug.log*")
$ExcludeDirs  = @(".git", "node_modules", ".vercel", "dist", "scratch", ".vite")
$roboParams = @($SourceDir, $TargetDir, "/E", "/XO", "/FFT", "/XF") + $ExcludeFiles + @("/XD") + $ExcludeDirs + @("/R:1", "/W:1", "/NP")
robocopy @roboParams
```
ثم يتم عمل `git push origin main` من مجلد الإنتاج، ليقوم Vercel بنشر النسخة المحدثة للبيئتين تلقائياً وبأمان تام.

---

## 6. استراتيجية التسعير والباقات المعتمدة (Commercial Pricing Guide)
- **الباقة الأساسية (Core ERP):**
  - **اشتراك سنوي (SaaS):** 9,500 إلى 12,000 ج.م / سنوياً (شاملة 3 مستخدمين).
  - **ترخيص دائم (On-Premise):** 25,000 إلى 35,000 ج.م (+ 20% عقد صيانة سنوي).
- **الموديولات التخصصية (إضافات سنوية):**
  - موديول المطاعم والكافيهات: + 6,000 إلى 8,000 ج.م / سنة.
  - موديول الهايبر ماركت والتجزئة: + 4,500 إلى 6,000 ج.م / سنة.
  - موديول المقاولات والمستخلصات: + 12,000 إلى 16,000 ج.م / سنة.
  - موديول الملاعب والنوادي الرياضية: + 8,000 إلى 10,000 ج.م / سنة.
  - موديول المستشفيات والمراكز الطبية: + 15,000 إلى 22,000 ج.م / سنة.
  - موديول المصانع والتصنيع: + 10,000 إلى 14,000 ج.م / سنة.
  - موديول الموارد البشرية المتقدمة: + 4,000 إلى 5,500 ج.م / سنة.
- **رسوم التأسيس ورفع البيانات والتدريب:** 3,000 إلى 8,000 ج.م (تدفع لمرة واحدة).

---
*تم اعتماد هذا التوثيق ليكون الدليل المرجعي الكامل والمحدث للمطورين ولمساعد الذكاء الاصطناعي.*