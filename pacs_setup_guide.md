# 🏥 دليل إعداد خادم الأشعة المحلي (PACS / Orthanc) وتشغيل الصور الحقيقية

يوضح هذا الدليل كيفية تثبيت خادم الأشعة المجاني **Orthanc** على جهازك الشخصي، وربطه ببرنامج **TriPro-ERP** لعرض صور أشعة حقيقية وتطبيق أدوات القياس والتباين عليها.

---

## 📥 الخطوة 1: تحميل خادم Orthanc PACS وتثبيته
1. اذهب إلى الموقع الرسمي للتحميل: [Orthanc Downloads](https://www.orthanc-server.com/download.php) أو قم بتحميل النسخة الخاصة بالويندوز مباشرة من: [Orthanc Windows Installer](https://www.orthanc-server.com/download-windows.php).
2. قم بتحميل وتثبيت الإصدار القياسي (Installer). أثناء التثبيت، اتبع الإرشادات الافتراضية.
3. بعد التثبيت، سيعمل الخادم كخدمة خلفية (Windows Service) بشكل تلقائي.

---

## ⚙️ الخطوة 2: السماح بالربط وتجاوز حماية CORS
لكي يتمكن متصفح الويب (الذي يعمل عليه برنامجنا على المنفذ `3000`) من قراءة الصور من خادم الأشعة (الذي يعمل على المنفذ `8042`) دون أن يمنعه المتصفح بسبب سياسة الأمان (CORS)، يجب ضبط الإعدادات كالتالي:

1. افتح مجلد تثبيت برنامج Orthanc (غالباً في المسار `C:\Program Files\Orthanc` أو `C:\Program Files (x86)\Orthanc`).
2. ابحث عن ملف الإعدادات باسم `Configuration.json` أو مجلد `Configuration` وافتح الملف الأساسي باستخدام مفكرة الويندوز (Notepad) كمسؤول (Run as Administrator).
3. ابحث عن الكلمة المفتاحية `"Headers"` وقم بتعديلها لتسمح بالاتصال من أي مكان كالتالي:
   ```json
   "Headers" : {
     "Access-Control-Allow-Origin" : "*",
     "Access-Control-Allow-Headers" : "Origin, X-Requested-With, Content-Type, Accept, Authorization",
     "Access-Control-Allow-Methods" : "GET, POST, PUT, DELETE, OPTIONS"
   }
   ```
4. احفظ الملف ثم أعد تشغيل خدمة Orthanc من قائمة الخدمات في ويندوز (Windows Services) أو أعد تشغيل جهازك.

---

## 📂 الخطوة 3: رفع صور أشعة حقيقية (DICOM) للخادم
1. افتح متصفح الويب واذهب للعنوان: `http://localhost:8042/app/explorer.html` (هذه هي واجهة خادم الأشعة).
2. اضغط على زر **Upload** في أعلى اليمين.
3. قم بسحب وإفلات أي ملف أشعة بصيغة `DICOM` (امتداد الملفات غالباً يكون `.dcm` أو بدون امتداد).
   * *💡 نصيحة:* إذا لم تكن تملك صور أشعة على جهازك، يمكنك تحميل عينات مجانية تماماً من موقع [Orthanc Demo Dataset](https://www.orthanc-server.com/demo/) أو من [DICOM Library](https://www.dicomlibrary.com/).
4. بمجرد الرفع، سيظهر اسم المريض والفرز الطبي في القائمة داخل واجهة Orthanc.

---

## 🖥️ الخطوة 4: تشغيل المعاينة الحقيقية في TriPro-ERP
1. افتح مديول الأشعة في برنامجك: **وحدة الأشعة والتشخيص التصويري**.
2. اضغط على زر **"كتابة التقرير"** لأي مريض.
3. سيقوم البرنامج بالاتصال تلقائياً بخدمة الأشعة المحلية على جهازك عبر الرابط `http://localhost:8042/instances`.
4. إذا وجد أي صور أشعة تم رفعها، سيقوم بعرضها فوراً وبشكل حقيقي بدلاً من الرسمة التوضيحية الافتراضية.
5. يمكنك الآن استخدام أدوات التكبير والتدوير والتباين والقياس بالمسطرة على صور حقيقية تماماً!
